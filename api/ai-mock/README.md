Placeholder for an optional serverless AI-mock endpoint (e.g. Vercel/Netlify function) if you'd
rather not use the client-side Gemini call in `src/features/ai-mock/AiMockTest.tsx`. Not required —
the app works fully offline without this.
